/** @deprecated Prefer DatasetFilterNav — kept for any leftover imports. */
export const CHIP_ROW =
  "flex min-w-0 flex-wrap gap-2";

export const DESKTOP_TABLE =
  "hidden max-w-full overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm md:block";

export const MOBILE_CARDS = "space-y-3 md:hidden";

/** Wrap wide tables so they scroll inside the card instead of expanding the page. */
export const TABLE_SCROLL =
  "max-w-full overflow-x-auto overscroll-x-contain [-webkit-overflow-scrolling:touch]";
